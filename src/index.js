/**
 * Get the computed style of a DOM element
 * 
 * @param {Element} element
 * @return {Object} {color, border, ...}
 * @api public
 *
 * @todo make it cross browser
 */
module.exports = getComputedStyle